package main

import "fmt"

const (
	GET    string = "GET"
	POST   string = "POST"
	PUT    string = "PUT"
	DELETE string = "DELETE"
	PATCH  string = "PATCH"
)

func main() {
	fmt.Println(GET)
}

// enumlar bu şekilde kullanılıyor.
